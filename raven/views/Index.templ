package views

templ Index(proxies []string) {
    <div>
        <div> Raven Proxy Lister </div>
        <div>
        for _, proxy := range proxies {
                <div> { proxy }</div>
        }
        </div>
    </div>
}